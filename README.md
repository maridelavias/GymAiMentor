# 💪 GymAiMentor

> Персональный AI-тренер в Telegram, который создает индивидуальные программы тренировок на основе ваших параметров и целей

[![Python](https://img.shields.io/badge/Python-3.11+-blue.svg)](https://www.python.org/)
[![Telegram Bot](https://img.shields.io/badge/Telegram-Bot-blue.svg)](https://core.telegram.org/bots)
[![DeepSeek](https://img.shields.io/badge/AI-DeepSeek-green.svg)](https://platform.deepseek.com/)

## 🎯 О проекте

**GymAiMentor** — это интеллектуальный Telegram-бот, который использует возможности AI (DeepSeek) для создания персонализированных программ силовых тренировок. Бот учитывает ваши физические параметры, цели, уровень подготовки и ограничения по здоровью, чтобы предложить оптимальный план тренировок для тренажерного зала.

### ✨ Основные возможности

- 🤖 **AI-генерация программ** — уникальные программы тренировок на основе DeepSeek
- 📋 **Интерактивная анкета** — сбор данных о пользователе через удобный интерфейс
- 🎯 **Три цели тренировок**: похудение, набор массы, поддержание формы
- 💪 **Акцент на группы мышц**: ноги, ягодицы, спина, плечи и руки, сбалансированная программа
- 🔄 **Вариации программ**: базовые упражнения, изоляция, акцент на силу или выносливость
- ❓ **Вопросы AI-тренеру** — получайте советы по тренировкам, питанию и восстановлению
- 📊 **Управление профилем** — просмотр и редактирование анкеты
- 💾 **Сохранение программ** — экспорт программ в файлы
- 📑 **История ответов** — доступ к ранее сгенерированным программам
- ⚡ **Rate limiting** — защита от перегрузки (30 секунд между генерациями)

## 🛠 Технологии

- **Python 3.11+** — основной язык разработки
- **python-telegram-bot 20.7** — фреймворк для работы с Telegram Bot API
- **DeepSeek** — AI-модель для генерации программ тренировок (deepseek-chat)
- **python-dotenv** — управление переменными окружения
- **psycopg** — подключение к PostgreSQL (опционально, для production)
- **reportlab** — генерация PDF-файлов (опционально)

## 📦 Установка

### Предварительные требования

- Python 3.11 или выше
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
- DeepSeek API Key (получить на [platform.deepseek.com](https://platform.deepseek.com/))

### Шаги установки

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/yourusername/GymAiMentor.git
cd GymAiMentor
```

2. **Создайте виртуальное окружение:**
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Создайте файл `.env` в корне проекта:**
```env
TELEGRAM_TOKEN=your_telegram_bot_token_here
DEEPSEEK_API_KEY=sk-your_deepseek_api_key_here

# База данных (опционально): при наличии используется Postgres вместо JSON-файлов
# DATABASE_URL=postgresql://user:pass@host:5432/dbname

# Опциональные настройки DeepSeek
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_TEMPERATURE=0.35
DEEPSEEK_MAX_TOKENS=5000
DEEPSEEK_TIMEOUT=60
DEEPSEEK_RETRIES=3
```

5. **Запустите бота:**
```bash
python main.py
```

## 🚀 Развертывание

### Docker

Проект включает `Dockerfile` для контейнеризации:

```bash
# Сборка образа
docker build -t gym-ai-mentor .

# Запуск контейнера
docker run -d \
  --name gym-ai-mentor \
  --env-file .env \
  -v $(pwd)/data:/app/data \
  gym-ai-mentor
```

### Northflank

Подробная инструкция по развертыванию на Northflank доступна в файле [`northflank-deploy.md`](northflank-deploy.md).

**Быстрый старт:**
1. Создайте проект на Northflank
2. Создайте Addon PostgreSQL (например, `gym-db`), привяжите его к Secret group
3. Создайте Combined Service и подключите GitHub-репозиторий
4. Настройте переменные окружения: TELEGRAM_TOKEN, DEEPSEEK_API_KEY и привяжите Secret group с переменными БД (в т.ч. `NF_GYM_DB_POSTGRES_URI`)
5. Запустите сервис (Persistent Volume опционален — при наличии Postgres данные хранятся в БД)

## 📁 Структура проекта

```
GymAiMentor/
├── app/
│   ├── agent.py          # AI-агент для работы с DeepSeek
│   └── storage.py        # Управление данными пользователей
├── bot/
│   └── telegram_bot.py  # Основная логика Telegram-бота
├── data/
│   └── users/           # JSON-файлы (если Postgres не настроен) и экспорт программ
├── docs/                # Документация проекта
├── main.py              # Точка входа приложения
├── requirements.txt     # Зависимости Python
├── Dockerfile           # Конфигурация Docker
├── .dockerignore        # Исключения для Docker
└── README.md            # Этот файл
```

## 🎮 Использование

### Команды бота

- `/start` — начать работу с ботом или сбросить анкету
- `/menu` — показать главное меню

### Процесс работы

1. **Запуск**: Отправьте `/start` боту
2. **Анкета**: Заполните данные о себе (имя, пол, возраст, рост, вес, цель, уровень подготовки, частота тренировок)
3. **Генерация**: Бот создаст персональную программу тренировок
4. **Дополнительно**: 
   - Задавайте вопросы AI-тренеру
   - Генерируйте новые программы с разными акцентами
   - Редактируйте параметры анкеты
   - Сохраняйте программы в файлы

### Примеры взаимодействия

```
Пользователь: /start
Бот: Привет! Я твой персональный фитнес-тренер GymAiMentor 💪🏼
     Помогу составить для тебя программу тренировок...
     Как тебя зовут?

Пользователь: Марина
Бот: Марина, выбери свою цель тренировок ⬇️
     [🏋️‍♂️ Набрать массу] [🏃‍♂️ Похудеть] [🧘 Поддерживать форму]

Пользователь: 🏋️‍♂️ Набрать массу
Бот: Укажи свой пол:
     [👩 Женский] [👨 Мужской]
...
```

## ⚙️ Конфигурация

### Переменные окружения

| Переменная | Описание | Обязательная | По умолчанию |
|-----------|----------|--------------|--------------|
| `TELEGRAM_TOKEN` | Токен Telegram-бота | ✅ Да | - |
| `DEEPSEEK_API_KEY` | API-ключ DeepSeek | ✅ Да | - |
| `DEEPSEEK_MODEL` | Модель DeepSeek | ❌ Нет | `deepseek-chat` |
| `DEEPSEEK_TEMPERATURE` | Температура генерации | ❌ Нет | `0.35` |
| `DEEPSEEK_MAX_TOKENS` | Максимум токенов в ответе | ❌ Нет | `5000` |
| `DEEPSEEK_TIMEOUT` | Таймаут запроса (сек) | ❌ Нет | `60` |
| `DEEPSEEK_RETRIES` | Количество повторов | ❌ Нет | `3` |
| `NF_GYM_DB_POSTGRES_URI` или `DATABASE_URL` | Строка подключения к PostgreSQL | ❌ Нет | — (при отсутствии используются JSON-файлы) |

### Настройка хранилища

- **Без БД:** данные сохраняются в `data/users/` в формате JSON (`{user_id}.json`). Подходит для локальной разработки.
- **С PostgreSQL:** если заданы `NF_GYM_DB_POSTGRES_URI` (Northflank) или `DATABASE_URL`, данные хранятся в таблице `user_data` (JSONB). На Northflank добавь Addon PostgreSQL и привяжи его к Secret group сервиса.

Для production с файловым хранилищем: Persistent Volume и регулярные бэкапы.

## 🔒 Безопасность

- Токены хранятся в переменных окружения (`.env`)
- Данные пользователей — в JSON-файлах или в PostgreSQL (при наличии переменной подключения)
- Rate limiting предотвращает злоупотребление API
- Валидация всех пользовательских данных

## 🐛 Troubleshooting

### Бот не запускается

- Проверьте, что `TELEGRAM_TOKEN` и `DEEPSEEK_API_KEY` установлены
- Убедитесь, что токены валидны и не истекли
- Проверьте логи на наличие ошибок

### Ошибки при генерации программы

- Проверьте подключение к интернету
- Убедитесь, что DeepSeek API доступен
- Проверьте лимиты API (количество запросов)
- Увеличьте `DEEPSEEK_TIMEOUT` если генерация долгая

### Данные пользователей теряются

- **При использовании Postgres:** проверьте, что `NF_GYM_DB_POSTGRES_URI` или `DATABASE_URL` заданы и доступны из сервиса; таблица `user_data` создаётся автоматически при первом обращении.
- **При файловом хранилище:** убедитесь, что директория `data/users/` существует и доступна для записи; при Docker проверьте монтирование volume и права доступа.
