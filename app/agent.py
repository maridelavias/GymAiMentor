import os
import re
import time
from typing import Optional

from gigachat import GigaChat
from gigachat.models import Chat, Messages, MessagesRole

from app.storage import load_user_data, save_user_data

GIGACHAT_MODEL: str = os.getenv("GIGACHAT_MODEL", "GigaChat-2-Max").strip()
GIGACHAT_TEMPERATURE: float = float(os.getenv("GIGACHAT_TEMPERATURE", "0.35"))
GIGACHAT_MAX_TOKENS: int = int(os.getenv("GIGACHAT_MAX_TOKENS", "5000"))
GIGACHAT_TIMEOUT: int = int(os.getenv("GIGACHAT_TIMEOUT", "90"))
GIGACHAT_RETRIES: int = int(os.getenv("GIGACHAT_RETRIES", "3"))


SYSTEM_PROMPT = """
Роль: Ты — опытный персональный тренер по силовым тренировкам и бодибилдингу (опыт более 8 лет), работающий в коммерческом фитнес-клубе. Ты создаёшь профессиональные, безопасные и эффективные программы тренировок для зала, ориентированные на долгосрочный прогресс, мотивацию и интерес к процессу.

Общие правила
* Используй строго Markdown.
* Никаких приветствий, вступлений, выводов или пояснений вне плана — только готовая программа.
* Стиль — профессиональный, чёткий, тренерский.
* Не задавай уточняющих вопросов — используй данные анкеты пользователя.
* Не используй RPE/RIR и формулировки «до отказа».
* Не включай травмоопасные или технически сложные упражнения для уровня пользователя.
* Подбирай адекватный тренировочный объём, позволяющий прогрессировать без перетренированности.

Ограничения по здоровью
* Если в анкете указаны ограничения, травмы или боль (спина, колени, плечи, шея и т.д.) — не включай упражнения, нагружающие проблемную зону, без безопасной альтернативы.
* При сомнениях выбирай вариант попроще (тренажёр вместо свободного веса, меньшая амплитуда, исключение движения).
* Если ограничения звучат как реабилитация или диагноз — в разделе «Заметки по прогрессии» кратко добавь: при сомнениях проконсультироваться с врачом или специалистом ЛФК.

Неполная анкета
* Если часть полей не заполнена (возраст, вес, уровень и т.д.) — используй безопасные допущения: уровень «начинающий», осторожные веса, без экстремальных объёмов.

КЛЮЧЕВОЕ УСЛОВИЕ — ВАРИАТИВНОСТЬ И ИНТЕРЕС
Ты обязан обеспечивать разнообразие тренировок, сохраняя биомеханику и тренировочную логику.
Обязательные принципы вариативности:
* ❗ Не повторяй один и тот же набор упражнений из недели в неделю, если пользователь не просил шаблон.
* Используй разные, но эквивалентные движения, например:
    * жим штанги → жим гантелей → жим в тренажёре
    * тяга штанги → тяга гантели → горизонтальный блок
    * присед → гакк-присед → жим ногами
* Чередуй:
    * свободные веса / тренажёры
    * двусторонние / односторонние упражнения
    * вертикальные / горизонтальные тяги и жимы
* Меняй:
    * диапазоны повторений (6–8 / 8–10 / 10–12 / 12–15)
    * порядок упражнений (база не всегда первая)
* Допускается использование умеренных методических приёмов, если уровень позволяет:
    * паузы в нижней точке
    * медленный негатив
    * изометрические удержания (без экстремальных техник и отказа)
Цель — чтобы программа не выглядела шаблонной, но оставалась логичной и прогрессивной.

Вес отягощений
* Всегда указывай рекомендуемый стартовый вес, исходя из:
    * пола
    * веса тела
    * возраста
    * уровня подготовки
    * цели
    * ограничений
* Вес должен быть:
    * реалистичным для зала
    * безопасным
    * позволяющим выполнить все подходы с чистой техникой
* Если диапазон повторений — указывай вес относительно него.

Формат плана
Структура дней
* Каждый тренировочный день — с новой строки, между днями пустая строка.
* Заголовок: **День 1 — Грудь и трицепс**

В начале дня
* Разминка 5–7 минут
    * лёгкое кардио
    * суставная мобилизация
    * активация целевых мышц
Упражнения
Формат строго такой:
- Жим гантелей лёжа — 3×10, отдых 90 сек., усилие: умеренно, рекомендуемый вес: ~18 кг на руку
Правила:
* Собственный вес — указывать явно
* Для базовых движений — отдых 90–120 сек.
* Для изоляции — 45–75 сек.
* Усилие привязывай к диапазону повторений: «умеренно» = мог бы сделать ещё 2–4 повторения; «тяжело» = последние 1–2 повторения даются с трудом.
В конце дня
* Заминка / растяжка 3–5 минут
    * основные проработанные группы

Сплит по частоте
* 2–3 тренировки — full body или upper/lower
* 4 тренировки — upper/lower или push/pull/legs
* 5+ тренировок — классический сплит по мышечным группам
Выбор сплита должен соответствовать цели и уровню.

Заметки по прогрессии (обязательный раздел)
В конце программы добавь раздел:
Заметки по прогрессии
* Как увеличивать вес:
    * +2–2.5 кг, если все подходы выполнены уверенно и с хорошей техникой
* Как снижать нагрузку:
    * уменьшение веса на 10–15% при усталости или дискомфорте
* Общие правила:
    * техника важнее веса
    * контроль движения
    * отсутствие резкой боли

Главная цель
Создать структурированную, безопасную и НЕ СКУЧНУЮ программу силовых тренировок, которая:
* ощущается как работа с живым тренером
* поддерживает мотивацию
* даёт понятный путь прогрессии
* не превращается в однотипный шаблон из недели в неделю
"""


QA_SYSTEM_PROMPT = """
Роль: Ты — персональный тренер и консультант высокого уровня (опыт более 8 лет), работающий с клиентами в формате индивидуальных консультаций и программирования нагрузок. Ты профессионально разбираешься в силовых тренировках, кардио, функциональном тренинге, беге, йоге, домашних тренировках, восстановлении, питании и спортивных добавках.

Твоя задача — ПОНЯТЬ НАМЕРЕНИЕ пользователя и действовать соответственно.

ОПРЕДЕЛЕНИЕ НАМЕРЕНИЯ (КЛЮЧЕВОЕ)
Программой считается только явный запрос: «программа», «план тренировок», «распиши тренировки на неделю», «хочу программу на …». Фразы вроде «что делать в зале», «какие упражнения на спину», «распиши на сегодня» — это консультация, давай советы и примеры, но не полную программу с днями и объёмом.

1. Если пользователь:
* задаёт вопрос
* просит совет
* интересуется техникой, питанием, добавками, прогрессией, ошибками
* спрашивает «что лучше», «как правильно», «имеет ли смысл»

→ ты отвечаешь в формате КОНСУЛЬТАЦИИ.
* ❗ НЕ создаёшь тренировочную программу.

2. Если пользователь ЯВНО просит программу или план тренировок (см. выше):

→ ты создаёшь ПРОГРАММУ тренировок под его запрос.

Если явного запроса на программу нет — программа НЕ создаётся.

ФОРМАТ ОТВЕТА (ВСЕГДА)
* Используй строго Markdown: списки, подзаголовки, выделение для структуры ответа.
* Без приветствий, вступлений и прощаний.
* Тон — профессиональный, спокойный, уверенный.
* Пиши так, как если бы консультировал клиента 1-на-1.
* Отвечай по делу, без воды.

РЕЖИМ КОНСУЛЬТАЦИИ
* Отвечай строго в рамках вопроса.
* Оформляй ответ в Markdown: списки, подзаголовки, краткие пояснения.
* Давай практические рекомендации и альтернативы.
* Можно указывать примерные веса, объёмы, частоту, но НЕ оформлять это как программу.
* Не используй RPE/RIR и формулировки «до отказа».
* Усилия описывай словами: «лёгко», «умеренно», «тяжело».

Запрещено в режиме консультации:
* Дни тренировок (День 1, День 2 и т.д.)
* Готовые планы с подходами и повторами
* Расписания по неделям

РЕЖИМ СОЗДАНИЯ ПРОГРАММЫ
* Создавай программу ТОЛЬКО по прямой просьбе пользователя.
* Тип программы может быть любым:
    * тренажёрный зал
    * домашние тренировки
    * бег / кардио
    * йога / мобильность
    * функциональный тренинг
* Учитывай все известные данные пользователя (пол, возраст, вес, цель, уровень, частота, ограничения).
* Если данных не хватает — делай безопасные допущения.

Оформление программы (как в основном генераторе):
* Заголовки дней (**День 1 — …**), разминка 5–7 мин и заминка 3–5 мин в каждом дне.
* Упражнения в формате: «— 3×10, отдых 90 сек., усилие: умеренно, рекомендуемый вес: …».
* В конце — раздел «Заметки по прогрессии» (как увеличивать/снижать нагрузку, техника важнее веса).
* Те же правила безопасности: без RPE/«до отказа», учёт ограничений по здоровью.

При создании программы:
* Соблюдай безопасность и соответствие уровню.
* Избегай травмоопасных и чрезмерно сложных вариантов.
* Программа должна быть логичной, разнообразной и выполнимой.

ПИТАНИЕ И БАДЫ
* Учитывай цель пользователя.
* Оперируй БЖУ, калориями и источниками нутриентов.
* По добавкам:
    * объясняй, зачем они нужны или не нужны
    * не обещай магических эффектов
    * указывай базовые дозировки и условия приёма
* ❗ КОНТЕКСТ ЗАПРОСА: если в предыдущих сообщениях пользователь просил план питания, меню на N дней или рацион — его следующие сообщения (ответы на твои уточняющие вопросы, дополнения) относятся к ЭТОМУ ЖЕ запросу. Отвечай именно планом питания/меню, а НЕ программой тренировок.

РАСЧЁТ КБЖУ (ОБЯЗАТЕЛЬНО при любых планах питания и рекомендациях по калориям/БЖУ)
Используй эти формулы и диапазоны. Не давай заниженный белок (1 г/кг — недостаточно для похудения с сохранением мышц и для активных людей).

1) Калории (ккал/день):
* Поддержание: ~25–30 ккал на кг текущего веса (ниже при низкой активности, выше при высокой).
* Похудение: дефицит 15–25% от поддержания (обычно 20–22 ккал/кг или расчёт: (вес_кг × 22–24) при умеренной активности).
* Набор массы: профицит 10–15% от поддержания.

2) Белок (г/день) — считать от текущего веса тела:
* Похудение: 1,5–2,0 г/кг (не менее 1,5 г/кг для сохранения мышечной массы).
* Набор массы: 1,6–2,2 г/кг.
* Поддержание при тренировках: 1,4–1,8 г/кг.
* Минимум для взрослых без тренировок: 1,2 г/кг.
Запрещено рекомендовать 1 г/кг или ниже при цели похудения или при активных тренировках.

3) Углеводы (г/день):
* Похудение и поддержание: примерно столько же граммов, сколько белка (углеводы ≈ белок в г/день).
* Набор массы: чуть больше белка (например, углеводы = 1,1–1,3× белок в граммах).

4) Жиры (г/день):
* Поддержание: не менее 1 г/кг веса тела.
* Похудение: на 10% меньше — не менее 0,9 г/кг.
* Набор массы: не менее 0,9–1 г/кг.
* Если по калориям жиры выходят выше — оставить по формуле; ниже минимума не опускаться.

ПРИНЦИПЫ
* Безопасность важнее скорости прогресса.
* Техника важнее веса.
* Рекомендации должны быть реалистичными.
* Если есть несколько рабочих вариантов — объясни различия.
* Не навязывай программы без запроса.

Вопросы вне экспертизы
* Если вопрос касается диагноза, травмы, острой боли, приёма лекарств или явно медицинский — не давай рекомендации по лечению. Кратко поясни и предложи обратиться к врачу или специалисту ЛФК.

Главная цель
Создать ощущение полноценной работы с опытным тренером, который:
* умеет консультировать
* умеет программировать нагрузки
* делает это только тогда, когда его об этом просят
"""



_RPE_PATTERNS = [
    r"\(?\s*RPE\s*=?\s*\d+(?:\s*-\s*\d+)?\s*\)?",
    r"\(?\s*RIR\s*=?\s*\d+(?:\s*-\s*\d+)?\s*\)?",
    r"\bдо\s+отказа\b",
    r"\bпочти\s+до\s+отказа\b",
]

def _strip_noise(text: str) -> str:
    """Убираем RPE/RIR/«до отказа», лишние пробелы и #/## заголовки."""
    out = text or ""
    # RPE/RIR
    for p in _RPE_PATTERNS:
        out = re.sub(p, "", out, flags=re.IGNORECASE)

    # заменить маркеры • на дефисы, x/* на ×
    out = re.sub(r"^\s*•\s+", "- ", out, flags=re.MULTILINE)
    out = re.sub(r"(\d)\s*[xX\*]\s*(\d)", r"\1×\2", out)

    # убрать HTML теги <br>, <p>
    out = re.sub(r"\s*<br\s*/?>\s*", "\n", out, flags=re.IGNORECASE)
    out = re.sub(r"</?p\s*/?>", "\n", out, flags=re.IGNORECASE)

    # убрать markdown заголовки # и ##
    out = re.sub(r"^\s*#{1,6}\s*", "", out, flags=re.MULTILINE)

    # косметика
    out = re.sub(r"\(\s*\)", "", out)
    out = re.sub(r",\s*,", ", ", out)
    out = re.sub(r"[ \t]{2,}", " ", out)
    out = re.sub(r"[ \t]+\n", "\n", out)
    out = re.sub(r"\n[ \t]+", "\n", out)
    out = re.sub(r"\n{3,}", "\n\n", out)

    return out.strip()

def _to_int(s) -> Optional[int]:
    try:
        return int(re.search(r"\d+", str(s)).group(0))
    except Exception:
        return None


class FitnessAgent:
    def __init__(self, token: str, user_id: str):
        self.token = token
        self.user_id = user_id
        self.user_data = load_user_data(user_id)

        phys = self.user_data.get("physical_data") or {}
        self._user_name: Optional[str] = (phys.get("name") or "").strip() or None

        self._phys_prompt = self._format_physical_data(phys)


    async def get_program(self, user_instruction: str = "", focus_group_override: Optional[str] = None) -> str:
        """
        Вернёт сгенерированную программу (Markdown), с учётом анкеты.
        user_instruction — пожелания (стиль, объём и т.д.).
        focus_group_override — при «Другая программа»: группа, выбранная в этом запросе (для блока с примерами и объёмом).
        """
        from asyncio import to_thread
        phys = self.user_data.get("physical_data") or {}
        phys_prompt = self._format_physical_data(phys, focus_group_override=focus_group_override)
        payload = Chat(
            messages=[
                Messages(role=MessagesRole.SYSTEM, content=SYSTEM_PROMPT),
                Messages(role=MessagesRole.USER, content=phys_prompt + (f"\n\nПожелания: {user_instruction}" if user_instruction else "")),
            ],
            temperature=GIGACHAT_TEMPERATURE,
            max_tokens=GIGACHAT_MAX_TOKENS,
            model=GIGACHAT_MODEL,
        )

        def _chat_sync():
            last_err = None
            for attempt in range(1, GIGACHAT_RETRIES + 1):
                try:
                    try:
                        with GigaChat(
                            credentials=self.token,
                            verify_ssl_certs=False,
                            timeout=GIGACHAT_TIMEOUT,
                            model=GIGACHAT_MODEL,
                        ) as giga:
                            resp = giga.chat(payload)
                            return resp.choices[0].message.content
                    except TypeError:
                        with GigaChat(
                            credentials=self.token,
                            verify_ssl_certs=False,
                            timeout=GIGACHAT_TIMEOUT,
                        ) as giga:
                            resp = getattr(giga, "chat")(payload, model=GIGACHAT_MODEL)
                            return resp.choices[0].message.content
                except Exception as e:
                    last_err = e
                    if attempt == GIGACHAT_RETRIES:
                        raise
                    time.sleep(1.5 * attempt)
            raise last_err or RuntimeError("GigaChat call failed")

        txt = await to_thread(_chat_sync)
        cleaned = _strip_noise(txt)
        final = self._with_name_prefix(cleaned)

        # сохраняем в историю и как последнюю программу
        hist = self.user_data.get("history", [])
        hist.append(("🧍 Запрос программы", "🤖 " + final))
        self.user_data["history"] = hist
        self.user_data["last_program"] = final
        self.user_data["last_reply"] = final
        save_user_data(self.user_id, self.user_data)
        return final

    def _qa_history_messages(self, current_question: str, max_turns: int = 6) -> list:
        """Последние пары вопрос–ответ из истории (только QA, без «Запрос программы») для контекста диалога."""
        hist = self.user_data.get("history", [])
        # пары (user, assistant); запись программы — ("🧍 Запрос программы", "🤖 ..."), пропускаем
        turns = []
        for user_msg, bot_msg in hist:
            if user_msg == "🧍 Запрос программы":
                continue
            u = user_msg[2:] if user_msg.startswith("🧍 ") else user_msg
            b = bot_msg[2:] if bot_msg.startswith("🤖 ") else bot_msg
            turns.append((u, b))
        # берём последние max_turns пар (не включая текущий вопрос)
        recent = turns[-max_turns:] if len(turns) > max_turns else turns
        messages = [
            Messages(role=MessagesRole.SYSTEM, content=QA_SYSTEM_PROMPT),
            Messages(role=MessagesRole.USER, content=f"Анкета:\n{self._phys_prompt}"),
        ]
        for u, b in recent:
            messages.append(Messages(role=MessagesRole.USER, content=u))
            messages.append(Messages(role=MessagesRole.ASSISTANT, content=b))
        messages.append(Messages(role=MessagesRole.USER, content=f"Вопрос (текущий):\n{current_question}"))
        return messages

    async def get_answer(self, question: str) -> str:
        """
        Краткий структурированный ответ/совет. Если явно просят план — можно выдать план (учитывая анкету).
        Учитывает последние реплики диалога (история QA), чтобы не терять контекст (например, запрос меню на 7 дней).
        """
        from asyncio import to_thread
        messages = self._qa_history_messages(question)
        payload = Chat(
            messages=messages,
            temperature=min(0.35, GIGACHAT_TEMPERATURE),
            max_tokens=min(2500, GIGACHAT_MAX_TOKENS),
            model=GIGACHAT_MODEL,
        )

        def _chat_sync():
            try:
                with GigaChat(credentials=self.token, verify_ssl_certs=False, timeout=GIGACHAT_TIMEOUT) as giga:
                    try:
                        resp = giga.chat(payload)
                    except TypeError:
                        resp = getattr(giga, "chat")(payload, model=GIGACHAT_MODEL)
                    return resp.choices[0].message.content
            except Exception as e:
                raise e

        txt = await to_thread(_chat_sync)
        cleaned = _strip_noise(txt).strip()

        # история
        hist = self.user_data.get("history", [])
        hist.append(("🧍 " + question, "🤖 " + cleaned))
        self.user_data["history"] = hist
        self.user_data["last_reply"] = cleaned
        save_user_data(self.user_id, self.user_data)
        return cleaned


    def _format_physical_data(self, d: dict, focus_group_override: Optional[str] = None) -> str:
        # базовая информация
        result = (
            f"Цель: {d.get('target') or 'не указана'}\n"
            f"Пол: {d.get('gender') or 'не указано'}\n"
            f"Возраст: {d.get('age') or 'не указано'} лет\n"
            f"Рост: {d.get('height') or 'не указано'} см\n"
            f"Текущий вес: {d.get('weight') or 'не указано'} кг\n"
            f"Желаемый вес: {d.get('goal') or 'не указано'} кг\n"
            f"Ограничения: {d.get('restrictions') or 'нет'}\n"
            f"Частота тренировок: {d.get('schedule') or 'не указано'}\n"
            f"Уровень: {d.get('level') or 'не указано'}"
        )
        
        # группа мышц: из override («Другая программа») или из профиля
        preferred_group = focus_group_override if focus_group_override is not None else d.get('preferred_muscle_group')
        if preferred_group and preferred_group not in ('сбалансированно', 'все группы мышц сбалансированно'):
            # расширенные рекомендации с конкретными примерами и правилами объёма
            exercises_details = {
                'грудь': {
                    'examples': 'жим штанги лёжа (классический, узким хватом), жим гантелей лёжа и под углом, отжимания (классические, на брусьях, с возвышения), кроссовер (снизу, сверху, по центру), сведение гантелей лёжа, жим в тренажёре',
                    'types': 'базовые жимы (штанга, гантели, тренажёры) и изолирующие (сведения, кроссовер)',
                    'volume': 'при акценте на грудь: 4-6 упражнений на грудь в тренировке; остальные группы — поддерживающий объём 2-3 упражнения'
                },
                'ноги': {
                    'examples': 'приседания (классические, фронтальные, болгарские, гакк, в Смите), выпады (вперед, назад, в сторону, проходкой), жим ногами под разными углами, разгибания/сгибания ног, зашагивания на платформу, приседания с паузой; икры: подъёмы на носки стоя, сидя, в тренажёре, поочерёдные на одной ноге',
                    'types': 'базовые многосуставные (приседания, выпады, жимы ногами), изолирующие (разгибания, сгибания) и икры в конце дня ног (1-2 упражнения)',
                    'volume': 'при акценте на ноги: 4-6 упражнений на ноги + 1-2 на икры в каждой тренировке ног; остальные группы — поддерживающий объём 2-3 упражнения'
                },
                'ягодицы': {
                    'examples': 'ягодичный мостик (классический, на одной ноге, с весом), отведения в тренажёре (стоя, на четвереньках), румынская тяга, выпады назад и в сторону, болгарские сплит-приседы, гиперэкстензии с акцентом на ягодицы, приседания сумо, становая тяга сумо',
                    'types': 'базовые (румынская тяга, приседания сумо) и изолирующие (мостики, отведения)',
                    'volume': 'при акценте на ягодицы: 4-6 упражнений на ягодицы в тренировке; остальные группы — поддерживающий объём 2-3 упражнения'
                },
                'спина': {
                    'examples': 'становая тяга (классическая, румынская, сумо), тяги штанги/гантелей в наклоне разными хватами, подтягивания (широким, средним, узким хватом), тяги верхнего блока (к груди, за голову), тяги горизонтального блока (к поясу, к груди), тяга Т-грифа, шраги (со штангой, гантелями), пуловеры',
                    'types': 'вертикальные тяги (подтягивания, тяги блока) и горизонтальные тяги (тяги в наклоне, блока к поясу)',
                    'volume': 'при акценте на спину: 4-6 упражнений на спину в тренировке; остальные группы — поддерживающий объём 2-3 упражнения'
                },
                'плечи': {
                    'examples': 'жимы штанги/гантелей (стоя, сидя), махи гантелей в стороны, махи в наклоне, подъёмы перед собой, разведения в тренажёре, жим Арнольда',
                    'types': 'базовые жимы и изолирующие движения для переднего, среднего и заднего пучков',
                    'volume': 'при акценте на плечи: 3-5 упражнений на плечи в тренировке; остальные группы — поддерживающий объём 2-3 упражнения'
                },
                'руки': {
                    'examples': 'БИЦЕПС: подъёмы штанги/гантелей, молотковые сгибания, скамья Скотта. ТРИЦЕПС: французский жим, разгибания на блоке, отжимания на брусьях, жим узким хватом',
                    'types': 'базовые (брусья, жимы) и изолирующие (сгибания, разгибания)',
                    'volume': 'при акценте на руки: 4-6 упражнений на руки в тренировке (2-3 бицепс, 2-3 трицепс); остальные группы — поддерживающий объём 2-3 упражнения'
                },
                'плечи и руки': {
                    'examples': 'ПЛЕЧИ: жимы штанги/гантелей (стоя, сидя), махи гантелей (в стороны, вперёд, в наклоне), тяги к подбородку, жим Арнольда. БИЦЕПС: подъёмы штанги/гантелей, молотковые, сгибания на скамье Скотта. ТРИЦЕПС: французский жим, отжимания на брусьях, разгибания на блоке',
                    'types': 'базовые (жимы плеч, брусья) и изолирующие (махи, сгибания, разгибания)',
                    'volume': 'при акценте на плечи и руки: 3-5 на плечи + 2-3 бицепс + 2-3 трицепс в тренировке; остальные группы — поддерживающий объём 2-3 упражнения'
                },
                'кор и пресс': {
                    'examples': 'планки (классические, боковые), подъёмы ног в висе/лёжа, скручивания, «мёртвый жук», роллауты с колесом, Pallof press, гиперэкстензии',
                    'types': 'статические (планки) и динамические (скручивания, подъёмы ног)',
                    'volume': '2-4 упражнения на кор в каждой тренировке (дополнение к основной программе)'
                },
                'функциональные и стабилизирующие': {
                    'examples': 'выпады с поворотом корпуса, приседания на одной ноге, тяги в наклоне одной рукой, упражнения с резинками, баланс на одной ноге',
                    'types': 'односторонние и стабилизационные движения',
                    'volume': '1-3 упражнения как дополнение к основной тренировке'
                },
            }
            volume_meta = (
                "ОБЪЁМ ЗА ТРЕНИРОВКУ: при акценте на группу — 4–6 упражнений на неё; "
                "поддерживающая нагрузка на остальные — 2–3 упражнения; "
                "общее количество упражнений за одну тренировку: 6–9. "
                "Включай 2–4 упражнения на кор/пресс в неделю; при дне ног — икры в конце (1–2 упражнения)."
            )
            details = exercises_details.get(preferred_group, {})
            result += (
                f"\n\n🎯 ═══ ГЛАВНЫЙ ПРИОРИТЕТ ПРОГРАММЫ ═══\n"
                f"Пользователь хочет МАКСИМАЛЬНЫЙ АКЦЕНТ НА {preferred_group.upper()}!\n\n"
                f"📋 ОБЯЗАТЕЛЬНЫЕ ТРЕБОВАНИЯ К ПРОГРАММЕ:\n\n"
                f"1️⃣ ОБЪЁМ НАГРУЗКИ:\n"
                f"   • {details.get('volume', f'При акценте: 4–6 упражнений на {preferred_group}; остальные группы — 2–3 упражнения')}\n"
                f"   • {volume_meta} (это ОБЩЕЕ ограничение объёма, не добавляется поверх основного)\n\n"
                f"2️⃣ СТРУКТУРА ТРЕНИРОВОК:\n"
                f"   • Основной фокус тренировки — {preferred_group}\n"
                f"   • Приоритет — базовые упражнения на {preferred_group}, но допускается предварительная активация или изоляция при необходимости\n"
                f"   • Обязательно сочетай базовые и изолирующие движения\n"
                f"   • Используй разные углы, хваты, стойки и варианты выполнения\n\n"
                f"3️⃣ ПРИМЕРЫ УПРАЖНЕНИЙ (для ориентира, НЕ как фиксированный список):\n"
                f"   {details.get('examples', f'разнообразные эффективные упражнения на {preferred_group}')}\n\n"
                f"🔄 ВАРИАТИВНОСТЬ:\n"
                f"   • Не повторяй один и тот же набор упражнений из недели в неделю\n"
                f"   • Допускается замена упражнений на эквивалентные по биомеханике\n\n"
                f"⚠️ ВАЖНО:\n"
                f"   • Примеры упражнений приведены для ориентира\n"
                f"   • Допускаются ЛЮБЫЕ безопасные и эффективные упражнения на {preferred_group}, соответствующие уровню пользователя\n"
                f"   • Минимум две тренировки в неделю должны иметь явный приоритет на {preferred_group}"
            )
        
        return result

    def _with_name_prefix(self, text: str) -> str:
        name = (self._user_name or "").strip()
        return (f"{name}, лови программу тренировок 💪🏼 Если тебе понравился этот план, не забудь сохранить ответ кнопкой ниже ⬇️\n\n" if name else "") + text